"""Main entry point for the final pivoted system.

The original polling prototype is not started here because the assignment
requires the pivot to be real rather than cosmetic.
"""

from server import run


if __name__ == "__main__":
    run()
